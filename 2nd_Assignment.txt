1. SELECT SALARY FROM EMPLOYEE QUALIFY RANK() OVER (ORDER BY SALARY DESC)=10
2. SELECT ROW_NUMBER() OVER (PARTITION BY WORKER_ID ORDER BY WORKER_ID) AS ROW_NUMBER FROM (SELECT WORKER_ID FROM WORKER 
	UNION ALL
	SELECT WORKER_REF_ID FROM BONUS) WHERE ROW_NUMBER=1
3A. SELECT ROW_NUMBER() OVER (ORDER BY WORKER_ID ) AS ROWNUMBER, * FROM WORKER WHERE (ROWNUMBER%2)=1 (ODD)
3B. SELECT ROW_NUMBER() OVER (ORDER BY WORKER_ID ) AS ROWNUMBER, * FROM WORKER WHERE (ROWNUMBER%2)=0 (EVEN)
4. SELECT * FROM DETAILS QUALIFY ROW_NUMBER() OVER (PARTITION BY SUFFIX ORDER BY SUFFIX) = 1
   SELECT DISTINCT SUFFIX FROM DETAILS
5. SELECT 'JAI1DE324EF$$%5^' AS C1, REGEXP_REPLACE(C1,'[^a-zA-Z]','',1,0,'i') AS C2;